# 2026-05-17 → 2026-05-18 새벽 (캡스톤 작업 일지)

## 5/17 ~ 5/18 새벽 완료 fix (8건)

### F-4 (3 Phase, 2차 시도 OK)
**목차 페이지 번호 우측 정렬** — 단일-run 케이스 (`\t` 가 run 시작에) 에서 페이지 번호 right edge 가 cell right inner 까지 미달.

- Phase 1 진단: 6/10 paragraph 는 cross-run path 정상, 4/10 단일-run path 두 오차 결합
- Phase 2 1차 fix: `EmbeddedTextMeasurer` 만 → SVG OK, Canvas NG
- Phase 3 본질: `WasmTextMeasurer` 도 동일 수정 (양 path 일관성)

변경: `src/renderer/mod.rs` (TextStyle::effective_margin_left), `paragraph_layout.rs` (두 곳), `text_measurement.rs` (양쪽 RIGHT+leader branch)

### 카테고리 A (자간 누적 그룹)
**따옴표 / 로마자 / 원 안 숫자** — PowerShell + console 결정적 단서 + 폰트 fallback 정공법.

- PowerShell 결정적 단서: HY 시리즈 폰트 모두 OS 등록 확인
- `rhwpDev` console 출력: FontLoader OS 폰트 감지에 HY 미포함 발견
- fix: FontLoader `OS_FONT_CANDIDATES` 확장 (HY 시리즈 + 휴먼 + 함초롬 + 한컴 + 맑은 고딕 Semilight) + `@font-face src` 에 `local()` 우선 + `font-display: swap`

변경: `rhwp-studio/src/core/font-loader.ts`

### Z (C-1: 숫자 폰트 자동 분리 제거)
**rhwp 가 숫자에 HCI Poppy 자동 적용 → 한글 본문 폰트 통일 정합**

- 본질: `detect_lang_category` 의 lang=1 (영문) 범위에 ASCII 숫자 0x0030-0x0039 포함 → 숫자가 영어로 분류되어 HCI Poppy 적용
- fix: lang=1 범위에서 숫자 제외 → `_ => 0` (한국어 fallback) 으로 한글 본문 폰트 사용

변경: `src/renderer/style_resolver.rs:385-393`, 테스트 정합 갱신

### X (사전검증 ① 정렬)
**hanging-indent 표 셀 안 ① 마커 단일 line 케이스 정렬**

- 진단 옵션 H2 (first_line_x clamp) 가설 NG → 정확 원인: `shrink_cell_padding_for_overflow` 가 hanging-marker 단일 line 도 padding shrink 적용
- fix: `shrink_cell_padding_for_overflow` 진입부에 hanging-marker 가드 (`indent < 0 && line_segs.len() == 1` → padding 그대로 반환)
- 진짜 본질 (estimate_text_width 5~15% 과대 추정) 은 본가 PR 영역으로 보류

변경: `src/renderer/layout/table_layout.rs:981-997`

### Y-2 (numbering "1." + 공백)
**본문 첫 항목 "1. 신뢰받는..." 의 "1." 누락 + trailing space 누락**

- 3 cell paragraph render path 발견: `table_layout.rs:1343`, `table_cell_content.rs:555`, `table_partial.rs:374` — 세 path 모두 `apply_paragraph_numbering` 호출 추가
- 추가 발견: "1.신뢰받는..." (공백 누락) — `apply_paragraph_numbering` Number/Outline 분기에 `NumberingHead.text_distance > 0` 시 trailing space 추가 (Bullet 패턴 정합)
- inner mech (커서 진입 시 "1." 안에 진입 가능) 은 `FieldMarkerType::Numbering` variant 부재 영역 — CRDT 보호 영역, 본가 PR 후보 도큐먼트화

변경: `paragraph_layout.rs:2920-2935`, `table_layout.rs:1343`, `table_cell_content.rs:555`, `table_partial.rs:374`

### Y-1 (참고2 박스 위치 정합 — 옵션 B 심화, 부분 완료)

#### 진단 흐름 (본질 추정 5번 정정 chain)
1. 1차 가설 (정렬) — process_controls vertical_offset stable sort → dump-pages 변화 없음, 효과 미정 (revert 후 재적용 시도)
2. 2차 가설 (round 통일) — text_measurement.rs:316 native `.round()` 제거 → dump-pages 변화 없음, revert
3. 3차 가설 (HY/휴먼 metric DB 부재) — alias 이미 존재 + entry 등록 확인 → 본질 아님
4. 4차 가설 (native vs WASM 차이) — 사용자 시각 22:57 WASM 빌드도 동일 page 17 결과 → 본질 아님
5. **★★★ 결정적 발견** (사용자 "표 지우기" 실험): 참고2 박스가 paragraph 시작 (page 16 상단) 에 정상 존재, 큰 어울림 표가 박스 위 겹쳐서 시각상 박스 사라진 것처럼 보임

#### 본질 (확정)
paragraph 0.407 안 controls=2:
- ci=0: 46x2 어울림 표 (`treat_as_char=false`, `vert_rel_to=Para`, `vert_offset=10.8mm`)
- ci=1: 1x3 "참고2" 박스 (`treat_as_char=true`, `vert_offset=0`)

원래 rhwp 처리: ci 순서 → ci=0 먼저 → paragraph 시작 분배 → ci=1 paragraph 마지막 page 잔류 (한컴 page 16 vs rhwp page 17)

#### fix 적용 (Step 1)
1. **process_controls vertical_offset stable sort** (`pagination/engine.rs:1011-1028`)
   - HWP ci 순서는 저장 순서, layout 순서 아님 → vert_offset 기준 stable sort
   - 단일 control paragraph 회귀 없음 (stable + 같은 offset 은 원본 순서 유지)

2. **`is_tac_table_inline` 조건 추가** (`height_measurer.rs:38-55`)
   - 기존: TAC 표 ≥ 2개 + 합산 폭 ≤ seg_width → inline
   - 추가: **TAC 1개 + non-TAC 어울림(TopAndBottom/Square) 표 혼재 + table_width ≤ seg_width → inline**
   - paragraph 0.407 ci=1 (1x3 TAC, 폭 169mm) 가 inline 처리 → tac_controls 에 추가 → paragraph_layout 의 line[0] inline → paragraph 시작 page 16 상단에 위치

#### 검증 결과 (Step 1)
- cargo test --lib renderer:: 483/483 ✓
- dump-pages 결과: `Table pi=407 ci=1` 가 PageItem 에서 사라짐 (inline 처리 확인)
- 사용자 "표 지우기" 실험: 참고2 박스 정확 위치 (page 16 상단) ✓
- 잔존 문제: anchored 큰 표가 박스 위 겹침 (vert_offset=10.8mm 적용 안 됨)

#### 잔존 본질 (5/18 trace 영역)
- `compute_table_y_position` (`table_layout.rs:1166+`) 의 `pushed = raw_y.max(y_start)` 클램핑 또는 wrap 영역 별도 path 가 vert_offset 무시
- trace 필요 (eprintln 으로 y_start, para_y, raw_y, pushed 값 dump)

변경: `pagination/engine.rs:1011-1028`, `height_measurer.rs:38-55`

## 5/17 ~ 5/18 새벽 학습 framework (14개)

### 1. native + WASM 두 path (F-4)
`EmbeddedTextMeasurer` (`compute_char_positions`) vs `WasmTextMeasurer` 양 path 일관성 필수. 한쪽만 수정 시 SVG OK / Canvas NG.

### 2. SVG 좌표 검증 ≠ Canvas 시각 정합 (F-4)
검증 5단계 필수: cargo check → test → SVG → wasm-pack build → **사용자 rhwp-studio 시각 검증**.

### 3. 단일-run vs cross-run 분기 (F-4)
`\t` 위치 (run 시작/중간/끝) 별 다른 처리 path. paragraph 콘텐츠 비결정적 요인.

### 4. targeted fix (한정 분기 조건) (F-4)
모든 `\t` 케이스 동일 본질 X. `has_content_after = seg_start_skipped < chars.len()` 분기로 회귀 0.

### 5. PowerShell + console 결정적 단서 (카테고리 A)
사용자 Windows + 한컴 환경 활용. OS 폰트 등록 여부 + `rhwpDev` console FontLoader OS 폰트 감지 → 결정적 단서.

### 6. 본질 분류 정정 패턴
가설 → 정밀 평가 → 정정. 1차 가설 NG 시 즉시 정정, 추정 누적 회피.

### 7. generic fix vs specific guard 분리
metric/estimate 영역 (X 진짜 본질, Y-1 quantization) → 본가 PR ★★★. layout 영역 (옵션 B 심화) → 캡스톤 fix 가능.

### 8. 시각 영역 vs inner mechanism 영역 분리 (Y-2)
"1." 시각 OK + 커서 진입 inner mech (FieldMarkerType::Numbering variant 부재) 분리. CRDT 보호 영역은 본가 PR.

### 9. F-4 동질 패턴 메트릭 영역 재현 (Y-1)
F-4 (RIGHT+leader native/WASM) 과 Y-1 (quantization native vs WASM) 동질 패턴. 양 path 미세 차이가 누적 효과 만듦.

### 10. 사용자 시각 평가가 자동 진단 정정 (Y-1)
클로드 dump-pages 결과 vs 사용자 rhwp-studio 시각 비교로 본질 영역 정정. "캡스톤 자동 진단" 만으로 부족, 사용자 시각이 결정적.

### 11. 본질 위치 정정 다중 (Y-1, 5번)
정렬 → round → metric DB → native/WASM 차이 → controls 순서. 추정 4번 NG 후 사용자 "표 지우기" 실험으로 진짜 본질 (anchored 표 위치 + TAC inline routing) 확정.

### 12. native vs WASM 차이 진짜 영역
quantization (line 1010 vs 618) + 휴리스틱 (font_size×0.5 vs JS Canvas) — 측정 차이 본질이 아닌 케이스도 많음. Y-1 의 경우 paragraph 내 controls 순서가 진짜 본질.

### 13. capstone scope framing 정정
"두 파일 정합" framing 좁음. 캡스톤 목표 = "모든 hwp 파일 한컴 정합" — 본가 PR 영역 확대.

### 14. 정밀 검증 패턴 — "회귀로 보였던 것이 실은 fix 효과 + 잔존 본질" (Y-1 결정적)
사용자 "표 지우기" 실험: 박스 사라진 게 아니라 큰 표가 덮음. fix 효과는 OK + 잔존 본질 (anchored 표 vert_offset) 만 남음. 깊은 정밀 평가 패턴.

## 본가 PR 후보 (5/22 이후 도큐먼트화)

### ★★★★ 본가 가치 최상
- **anchored shape vert_offset 처리 정밀화** (Y-1 잔존 본질) — `compute_table_y_position` 의 wrap 영역 처리
- **paragraph_layout TAC inline routing** (Y-1 옵션 B 심화) — `is_tac_table_inline` 조건 확장
- **FontLoader OS 폰트 + @font-face local()** (카테고리 A) — 한국어 한컴 폰트 전반 영향

### ★★★ 본가 가치 높음
- **Z (detect_lang_category 숫자 분리)** — 한글/영어 분류 정확도
- **F-4 (RIGHT+leader native/WASM 일관성)** — 두 path 정합

### ★★ specific guard (캡스톤 scope, 본가 PR 후보 X)
- **X (hanging-marker guard)** — `shrink_cell_padding_for_overflow` 특정 케이스
- **Y-2 공백** — Bullet 패턴 mirror

### 본가 영역 보류 (정직 framing)
- **estimate_text_width 5~15% 과대 추정** (X 진짜 본질) — 메트릭 영역
- **FieldMarkerType::Numbering variant 부재** (Y-2 inner mech) — CRDT 보호 영역
- **폰트 글리프 미세 차이** (F-6/F-8) — 본가 메트릭 영역

## 작업 환경
- 작업 브랜치: `local/capstone-fixes`
- 권한 정책: CAPSTONE-WORKFLOW.md 의 5/16 영구화 + 5/17 시각 검증 필수 단계 적용
- 검증 절차: 학습 2 의 5단계 (cargo check → test → SVG → wasm-pack build → **사용자 시각 검증**)

## 5/18 진행 계획

1. **현재 fix 일괄 커밋** (단위 분리, 안전 보존)
2. **사용자 push** (위험 명령 정책)
3. **Y-1 잔존 본질 trace** — `compute_table_y_position` 호출 시 y_start/para_y/raw_y/pushed dump
4. **anchored 표 vert_offset fix 시도** (4시간 한계, NG 시 옵션 P fallback)
5. **5/22 마감 prep** — 발표 내용 정리, docs 정리
