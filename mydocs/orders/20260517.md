# 2026-05-17 (캡스톤 작업 일지)

## 5/17 완료 fix (1건)

### F-4 (3 Phase, 2차 시도 OK)
**목차 페이지 번호 우측 정렬** — 단일-run 케이스 (`\t` 가 run 시작에) 에서 페이지 번호 right edge 가 cell right inner 까지 미달.

#### Phase 1 — 정밀 진단 (옵션 B)
- 공직기강 hwp 목차 = 표 셀 안 10개 paragraph, 모두 `ps_id=195`, 같은 인라인 탭 (`high_byte=2 fill_low=3` = RIGHT+leader)
- 6/10 paragraph (pi=2,4,6,7,8,9) 은 cross-run path 실행 → 정확한 정렬
- 4/10 paragraph (pi=0,1,3,5) 는 단일-run path → `body_right = available_width - line_x_offset` 가 effective_margin_left 미포함, seg_w 가 leading space 미포함 → 두 오차 결합

#### Phase 2 — 1차 fix (NG)
- TextStyle 에 `effective_margin_left` 필드 추가
- paragraph_layout.rs 의 두 위치 (est_x 루프, render 루프) 에서 `text_style.effective_margin_left = effective_margin_left` 설정
- text_measurement.rs `(2, _) high2_RIGHT_leader` branch 에 targeted fix
- 검증: cargo test renderer 483/483 통과, SVG right edge 666.58 통일 확인
- **그러나 rhwp-studio 시각 NG** (사용자 캡쳐 확인)

#### Phase 3 — 본질 식별 (2차 fix OK)
- 결정적 발견: rhwp 에 **compute_char_positions 두 구현 존재**:
  - `EmbeddedTextMeasurer` (text_measurement.rs:319-476) — 네이티브 CLI (`rhwp export-svg`)
  - `WasmTextMeasurer` (text_measurement.rs:759-903) — **WASM Canvas (rhwp-studio)**
- 1차 fix 는 `EmbeddedTextMeasurer` 만 수정 → 네이티브 SVG export 만 OK, Canvas 미반영
- 2차 fix: `WasmTextMeasurer.compute_char_positions` 의 `2 if fill_low != 0` (RIGHT + leader) branch 에 동일 targeted fix 적용
- wasm-pack build --target web --dev → 사용자 시각 검증 OK

#### 변경 파일
- `src/renderer/mod.rs`: TextStyle 에 `effective_margin_left: f64` 필드 + Default 0.0
- `src/renderer/layout/paragraph_layout.rs`: 두 곳 (est_x 루프, render 루프) 에서 effective_margin_left 설정
- `src/renderer/layout/text_measurement.rs`: 양쪽 구현 (Embedded + Wasm) 에 동일 targeted fix

## 5/17 학습 (framework)

### 학습 1: rhwp 에 native + WASM 두 path 가 존재 (★ 핵심)
- 코드: `EmbeddedTextMeasurer` (target ≠ wasm32) vs `WasmTextMeasurer` (target = wasm32)
- 둘 다 `Measurer` trait 의 `compute_char_positions` 구현
- char_width 함수가 다름:
  - Native: `measure_char_width_embedded` (Rust 임베디드 폰트 메트릭)
  - WASM: `js_measure_text_width` (브라우저 JS measureText 브리지)
- **`compute_char_positions` / `\t` 처리 / char_width 영역 수정 시 반드시 양쪽 동시 수정**
- 네이티브 CLI (`rhwp export-svg`) 가 OK 라고 WASM Canvas (rhwp-studio) 가 OK 라는 보장 X

### 학습 2: SVG 좌표 검증 ≠ Canvas 시각 정합
- Native CLI `rhwp export-svg` 출력의 좌표 비교만으로는 rhwp-studio Canvas 시각 정합 보증 X
- 검증 절차 갱신:
  1. cargo check (native + wasm32)
  2. cargo test --lib renderer:: + layout::
  3. SVG 좌표 비교 (native path 검증)
  4. wasm-pack build --target web --dev
  5. **사용자 rhwp-studio 시각 검증 (Canvas path 검증, 최종)**
- 4 단계 + 5 단계 누락 시 잘못된 OK 판정 가능

### 학습 3: 단일-run vs cross-run 분기 패턴
- `\t` 의 위치에 따라 다른 코드 path:
  - `\t` 가 run **끝** → cross-run path (paragraph_layout.rs:1414+, pending_right_tab)
  - `\t` 가 run **시작/중간** → compute_char_positions 안 in-run \t 처리
- 두 path 의 alignment 정합성 필수 — 한쪽만 정확하면 paragraph 별 visual 차이 발생
- HWP 의 `\t` 위치 결정은 CharStyle 변경에 따른 run 분할 결과 — paragraph 콘텐츠 길이/구성 등 비결정적 요인

### 학습 4: targeted fix (한정 분기 조건)
- 모든 `\t` 케이스 가 동일 본질 X
- `\t` 뒤에 content 있는 경우 (단일-run 페이지번호): cell right inner 정렬, leading space 포함 seg_w
- `\t` 뒤 trailing space 만 있는 경우 (cross-run 직전 run): 원본 path 유지 (다음 run 의 cross-run logic 가 처리)
- `has_content_after = seg_start_skipped < chars.len()` 분기로 회귀 0

## 보류

### F-4 검증 후 추가 발견 (사용자 검토)
1. **따옴표 글리프 차이** (목차 4번 항목) — PUA 매핑 가능성, isolated, A-4/F-1 패턴 적용 가능
2. **Ⅰ Ⅱ Ⅲ 로마자 위치 미세 차이** (목차 첫 항목) — 자간 누적 의심
3. **원 안 숫자 줄 정렬 어긋남** (8쪽 표 안 ① 등) — 자간 누적 의심
4. **참고2 페이지 밀림 + "1." 누락** (16쪽) — D-2 페이지 분할 누적 영역

본질 분류:
- 따옴표 (1): isolated, 빠른 fix
- 로마자/원 안 숫자/참고2 (2,3,4): 카테고리 A (자간 누적) 의 같은 본질 가능성, 한 번 풀면 셋 다 해결

## 작업 환경
- 작업 브랜치: `local/capstone-fixes`
- 권한 정책: CAPSTONE-WORKFLOW.md 의 5/16 영구화 정책 적용
- 검증 절차: 학습 2 에 정의된 5 단계
- 5/17 권한 추가: 사용자 시각 검증 필수 단계화 (학습 2)
