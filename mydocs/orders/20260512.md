# 2026-05-12 (캡스톤 작업 일지)

## 완료
- **A-7**: HWPX 글자 drop shadow 파싱 추가 (`rhwp/src/parser/hwpx/header.rs` `<hp:shadow>` arm 에 `offsetX`/`offsetY` 속성 파싱 2 줄 추가)
  - 영향: 스마트행정팀 바인더 1쪽 표지 타이틀 "스마트행정팀 바인더 자료"
  - 검증: cargo test parser::hwpx 23/23, renderer 483/483, wasm-pack --dev, 사용자 시각 검증 OK
- **B-5**: HWPX paragraph 의 비-인라인 control sentinel push 누락 fix (`rhwp/src/parser/hwpx/section.rs` 5곳 +5줄)
  - 본질: `<hp:ctrl><hp:colPr/></hp:ctrl>` 의 ColumnDef, `<hp:ctrl><hp:pageNum/></hp:ctrl>` 의 PageNumberPos 가 `text_parts.push("\u{0002}")` 누락 → char_offsets gap_before 부족 → `Paragraph::control_text_positions()` fallback 으로 인라인 Picture 가 paragraph 끝으로 밀림.
  - 영향: 스마트행정팀 바인더 1쪽 표지 — 인라인 라벨 이미지 위치 좌측 회복
  - 검증: cargo test parser::hwpx 23/23, renderer 483/483, dump-pages 페이지 1-3 fix 전후 동일, 사용자 시각 검증 OK
- **A-4**: HWPX PUA 글머리표 매핑 추가 (`rhwp/src/renderer/layout/paragraph_layout.rs` map_pua_bullet_char +3 entries)
  - `0xF007E → ■` (Supplementary PUA-A 저영역 0xF0000-0xF00CF, 29회 사용)
  - `0xF02FB → ▶` (잠정)
  - `0xF080C → ■` (잠정)
  - 진단 학습: 1차 시도에서 `0x7E` (Basic PUA) 잘못 매핑 → SVG 검증으로 정확한 영역 (`0xF007E`) 식별 후 정정
  - 검증: cargo test renderer 483/483, SVG 검증 (⊠ 0 hits, ■ 2 hits), 사용자 시각 검증 OK
- 워크플로우 갱신: `CAPSTONE-WORKFLOW.md` 검증 절차 / 절대 금지 / 버그 목록 (A-7 추가). 자동 SVG 비교 폐기 → `wasm-pack build --target web --dev` 매 수정.

## 진단 완료 (다음 세션 fix)
- **E-1 ★ (최우선)**: **인라인 라벨(treatAsChar Table) 두 번 렌더링**.
  - 본질: paragraph 39 (코로나 백신 예약 [유연성] 등) 의 inline `<hp:tbl>` treatAsChar=true 가 dump-pages 결과에 `Table pi=N ci=0 tac=true vpos=X` + `PartialParagraph pi=N lines=0..1 vpos=X` 로 **같은 vpos 에 둘 다** 표시. SVG 에 "유연성" 글자 시퀀스가 두 번 연속 출력 확인.
  - fix 위치 후보: `rhwp/src/renderer/typeset.rs` 또는 `rhwp/src/renderer/layout.rs` 의 `is_tac_table_inline=true` 분기 — 인라인 표는 별도 Table entry 만들면 안 됨.
  - 시각 임팩트 매우 큼 (모든 본문 박스 라벨 영향).

## 신규 발견 (다음 세션 분석)
- **A-3** (`< >` 꺾쇠 누락): paragraph 782 "&lt; 공통인프라* 비용 배분 &gt;" 의 quick_xml 디코딩 후 ASCII `<` `>`. SVG escape_xml 작동 확인됐으나 사용자 시각상 누락. char-by-char SVG 위치 매칭 진단 필요.
- **A-5** (▶ → □): paragraph 위치 매칭 미확정. U+F02FB 가 정답 char 인지 사용자 캡쳐 페이지 식별 후 정정.
- **A-4 디테일** (■ 안 화살표): 한컴 ■ 안 잘려나간 투명 화살표. PUA 매핑 정상화 후에도 남음 — 별도 글리프 overlay 처리 영역.
- **C-1** (한글/숫자 폰트 분리): 본문 "VM 당 2,453,594원" 숫자만 HCI Poppy. CharShape 의 lang-specific 폰트 처리 영역.
- **D-2** (목차 쪽번호 누락): 한컴은 목차 페이지(4,5,6) 에도 쪽번호 표시, rhwp 누락. page_number.rs / pagination/engine.rs 영역. 진단 결과 isolated 아님 (paragraph 375 newNum 위치, page 7=1 리셋 메커니즘, B-1 누적 결합).
- **B-1** (페이지 분할 누적): 한글 2024 "참고2) 클라우드 기반..." 이 다음 페이지로 넘어가지만 rhwp 같은 페이지 유지 → 전체 페이지 수 차이. 누적 효과 영역, 큰 작업.

## 다음 세션 첫 작업
**E-1 (라벨 중복, typeset/layout 분기) — 시각 임팩트 최대**.
진행:
1. typeset.rs / layout.rs 의 inline TAC Table 분기 read
2. 별도 Table entry 가 만들어지는 경로 식별
3. is_tac_table_inline=true 인 경우 entry skip fix
4. cargo check + test + wasm-pack + 사용자 시각 검증

## 작업 환경 메모
- 작업 브랜치: `local/capstone-fixes`
- 원격: origin (HaimLee-4869/rhwp). upstream (edwardkim/rhwp) 에 push 금지.
- 검증 절차: `cargo check --lib` → `cargo test --lib <module>` → `wasm-pack build --target web --dev` → 사용자 브라우저 캡쳐 비교.
- 자동 SVG 비교 도구 폐기. 매 수정마다 wasm --dev 빌드 + 브라우저 검증.
- 한 세션 = 한 버그 (또는 같은 카테고리 묶음). 회귀 방지.
