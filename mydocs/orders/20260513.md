# 2026-05-13 (캡스톤 작업 일지 — 5/12-13 사이클 마무리)

## 5/12-13 사이클 완료 fix (5건)

- **A-7** (5/12 완료, push ✅): HWPX 글자 drop shadow `offsetX`/`offsetY` 파싱 추가 — `rhwp/src/parser/hwpx/header.rs` `<hp:shadow>` arm. 영향: 스마트행정팀 바인더 1쪽 표지 타이틀.

- **B-5** (5/12 완료, push ✅): HWPX paragraph 의 비-인라인 control sentinel push 누락 — `rhwp/src/parser/hwpx/section.rs` 5곳. ColumnDef / PageNumberPos 의 `text_parts.push("\u{0002}")` 누락이 `Paragraph::control_text_positions()` fallback 으로 인라인 Picture 를 paragraph 끝으로 밀어내던 본질. 영향: 표지 인라인 라벨 이미지 좌측 정렬 회복.

- **A-4** (5/12 완료, push ✅): HWPX PUA 글머리표 매핑 추가 — `rhwp/src/renderer/layout/paragraph_layout.rs` `map_pua_bullet_char()` 에 `0xF007E => ■` (Supplementary PUA-A 저영역, 29회 사용), `0xF02FB => ▶` (잠정), `0xF080C => ■` (잠정). 1차 시도 영역 오인 (Basic PUA 0x7E) 정정 학습.

- **E-1** (5/13 완료): 인라인 TAC Table 두 번 렌더링 fix — `rhwp/src/renderer/typeset.rs:1797-1808` `place_table_with_text()` 에 `is_inline_tac` 가드 추가. paragraph_layout 의 inline 경로 + 별도 PageItem::Table entry 가 모두 발화하던 본질. dump-pages 결과 `Table pi=N tac=true` entry 가 inline TAC 한정 skip. 영향: 본문 라벨 ("유연성", "안정성") 중복 제거. SVG 검증: 유 6→4, 연 4→2, 성 6→3, 안 4→3.

- **F-2 / F-7** (5/13 완료): U+F02FB 매핑 정정 — `rhwp/src/renderer/layout/paragraph_layout.rs` `0xF02FB => ▶ (U+25B6)` → `▸ (U+25B8)`. 한컴 캡쳐 작은 삼각형 정합. HWP 파일 페이지 9 SVG: ▶ 0회 → ▸ 14회.

## 진단/발견 (다음 세션 — 9건)

### 폰트 영역 (rhwp 코드 fix 아님, 주무관님 요청 후 결정)
- **F-1**: U+F02B1~F02C4 한컴 자체 PUA 글리프 (사각 안 숫자) — Task #509 매핑 ①~⑳ 과 회귀 충돌, 한컴 폰트 (HY견고딕 등) 임베딩 영역
- **F-6**: U+25CA ◊ LOZENGE 표준 char — 한컴 폰트의 ◇ WHITE DIAMOND 글리프, 폰트 fallback 영역
- **F-8**: 폰트 메트릭 미세 차이
- **C-1**: 숫자에 HCI Poppy 자동 적용 — **rhwp 가 잘못 적용하는 것 (임베딩 X)**. 매핑 분리 정정 영역.

### 레이아웃 영역
- **F-4**: 목차 페이지 숫자 정렬 (tab + leader char) — 추가 진단 필요, **isolated 가능성** (다음 세션 우선)
- **F-5**: U+F007E 매핑 충돌 — 첫 미션 파일 (HWPX) ■ vs 본 파일 (HWP) ▣. 같은 PUA 두 파일 다른 한컴 글리프. 한컴 PDF 추가 시각 분석 후 결정.

### 페이지 분할 누적 (B-1 영역, 5/18-21 정면 돌파)
- **F-3**: 「참고2」 헤더 페이지 위치 — B-1 누적
- **B-1**: 스마트행정팀 목차 마지막 페이지 분할 — 누적 효과

### 페이지 번호
- **D-2**: 목차 페이지 쪽번호 누락 + 본문 시작 번호 차이 — page_number.rs / pagination/engine.rs, isolated 아님

## 다음 세션 (5/13 이후) 우선순위
1. **F-4** (목차 페이지 숫자 정렬) — 진단 추가 필요, isolated 가능성 — 첫 작업 후보
2. **B-1 정면 돌파** — 5/18-21 일정
3. **주무관님 답변 대기** 후 폰트 영역 (F-1, F-6, F-8) 결정
4. **C-1** (HCI Poppy 분리) — 매핑 분리 영역, isolated 가능성

## 폰트 정책 (CAPSTONE-WORKFLOW.md 신규 섹션)
- **HCI Poppy 임베딩 금지** (C-1 본질이 rhwp 자동 적용 문제)
- **함초롬 그대로 사용** (이미 작동)
- **한컴 자체 PUA 폰트 (HY견고딕 등) 주무관님께만 요청**

## 작업 환경
- 작업 브랜치: `local/capstone-fixes`
- 원격: origin (HaimLee-4869/rhwp). upstream (edwardkim/rhwp) push 금지.
- 검증 절차: cargo check → 관련 cargo test → wasm-pack --dev → 사용자 브라우저 캡쳐 비교.
- read-only 진단 명령 자동 실행 (사용자 정책).

## 5/12-13 학습
- **진단 정정 패턴**: A-4 1차 시도에서 PUA 영역 오인 (Basic 0x7E vs Supplementary 0xF007E) → SVG 검증 후 정정. 향후 진단에 char-by-char SVG 위치 매칭 필수.
- **isolated 식별 우선순위**: 진단 결과 isolated 인 fix 만 한 세션에 적용 (D-2 같이 결합 효과는 보류). 정직 보고로 시간 절약.
- **두 파일 검증**: 같은 PUA 가 두 파일에서 다른 한컴 글리프 (F-5) — 매핑 fix 한계 영역, 폰트 임베딩으로 해결.
