# 2026-05-18 (캡스톤 작업 일지)

## 5/18 완료 fix (3건)

### Y-1 후속 (참고2 박스 layout 완성, 5/17 Step 1 의 layout side)

**5/17 까지의 진단**: paragraph 0.407 의 controls (어울림 46x2 표 + 1x3 TAC 참고2 박스) 가 ci 순서로 처리되어 박스가 paragraph 끝 페이지 잔류. process_controls vert_offset stable sort + `is_tac_table_inline` 조건 확장 으로 pagination 측 fix 완료.

**5/18 추가 본질**: `is_tac_table_inline` 가 true 인 박스가 tac_controls 로 합류하지만, **paragraph_layout 의 inline TAC 분기에 `Control::Table` 케이스가 없어** 박스 cell content 가 렌더 안 됨. 또한 `layout_partial_paragraph` 진입 조건 `has_real_text` 가 빈 문단 (text_len=0) 차단해 host paragraph 의 inline TAC 가 layout pipeline 진입 못함.

#### fix 적용 (Step 1 layout 후속)
1. **`has_inline_tac` 가드 추가** ([layout.rs:2674-2705](rhwp/src/renderer/layout.rs#L2674-L2705))
   - 기존: `if has_real_text { layout_partial_paragraph(...) }`
   - 추가: `let has_inline_tac = composed.tac_controls.is_empty() == false; if has_real_text || has_inline_tac { ... }`
   - 빈 문단의 inline TAC (참고2 박스) 가 layout pipeline 진입

2. **`Control::Table` inline 분기 추가** ([paragraph_layout.rs:~2272](rhwp/src/renderer/layout/paragraph_layout.rs))
   - 기존: inline TAC 분기 Form/Shape/Picture/Equation 만 처리
   - 추가: `Control::Table(tbl) if tbl.common.treat_as_char` 분기 — `layout_table` 호출로 cell content 렌더
   - inline_x_override 로 정확한 x 위치 (paragraph 시작 + 누적 advance) 지정

#### 검증
- cargo test --lib: 1230/1230 ✓
- 사용자 시각 검증: 공직기강 page 16/17 박스 정상 위치 + cell content "참고2 도정 혁신방안별 실행 부서" 표시 OK ✓

변경: `src/renderer/layout.rs`, `src/renderer/layout/paragraph_layout.rs`

### 페이지 분할 정합 (Step 2 — pi=407 LAYOUT_OVERFLOW 31.2px)

**증상**: 공직기강 viewer page 16 에 anchored 46x2 표가 23 행 (0..23) 모두 squeeze 들어가 page bottom 31.2px 초과. 한컴은 22 행 (0..22) 만 fit 하고 row 22 ([2] 산하 공공기관장 채용 검증 강화 헤더) 를 page 17 로 이동.

**진단 흐름**:
1. `engine.rs paginate_table_control / split_table_rows` 에 `v_offset_px` 차감 로직 있음 (line 1693-1703) — 정상
2. 그러나 my fix 의 trace 가 fire 안 함 → 디폴트 pagination 이 Paginator 가 아님 발견
3. 본질: `rendering.rs:1142-1170` 가 **`TypesetEngine` 을 디폴트 pagination 으로 사용** (Paginator 는 `RHWP_USE_PAGINATOR=1` env 시 fallback)
4. typeset.rs 의 `typeset_block_table` split 로직 (line 2029-2032) 에 **v_offset_px 차감 누락** → engine.rs 와 동질 fix 누락

**Fix**: typeset.rs split 의 `page_avail` 계산에 `v_extra` 추가 ([typeset.rs:2034-2057](rhwp/src/renderer/typeset.rs)):
```rust
let v_offset_px = if !table.common.treat_as_char
    && matches!(table.common.vert_rel_to, VertRelTo::Para)
    && table.common.vertical_offset > 0
{ hwpunit_to_px(...) } else { 0.0 };
let v_extra = if !is_continuation && cursor_row == 0 && content_offset == 0.0 { v_offset_px } else { 0.0 };
let page_avail = (table_available - st.current_height - caption_extra - v_extra).max(0.0);
```

#### 검증 결과
- dump-pages: `pi=407 rows=0..23 / 23..46` → `pi=407 rows=0..22 / 22..46` ✓ ([2] 헤더 page 17 로 이동)
- LAYOUT_OVERFLOW 31.2px (page=18 PartialTable) → **해소** ✓
- 스마트행정팀 hwpx PartialTable 5개 (pi=13, 79, 154, 195, 266) — 회귀 0 ✓
- cargo test --lib: 1230/1230 ✓
- 사용자 시각 검증: page 16/17 페이지 분할 한컴 정합 OK ✓

변경: `src/renderer/typeset.rs`

### C8 + C9 (좁은 구두점 폭 분류 native + WASM 동기화)

**증상**: 휴먼명조 폰트 문단에서 가운뎃점 (U+2027), 여는 따옴표 (U+2018), 닫는 따옴표 (U+2019) 뒤 공백 과대 (한컴 대비 ~4px).

**진단 흐름**:
1. SVG 좌표 측정: 휴먼명조 ‧ advance = 10px @ font-size 20px = **0.5em** (한컴은 약 0.3em)
2. 폰트 메트릭 DB 정밀 점검 (`inspect_punct_widths.rs`):
   - **휴먼명조 DB 메트릭**: U+2018/U+2019 모두 1.000em (fullwidth) — 잘못 기록
   - **HY중고딕/HY신명조/HY견명조**: 동일 패턴 (DB fullwidth 잘못 기록)
   - **함초롬바탕/Pretendard/맑은 고딕**: DB narrow 정확 기록 (0.22-0.32em)
3. 원인:
   - DB fullwidth → `is_halfwidth_punct` 분기 (U+2018..=U+2027 range) 가 em/2 = 0.5em 강제
   - 휴먼명조 U+2027 글리프 DB 미수록 → 폴백 `font_size * 0.5` → 동일 0.5em 과대

**Fix C8 (native — text_measurement.rs)**:
1. **`is_narrow_punctuation` 에 U+2018/U+2019/U+2027 추가** (line 1138-1148) — DB 미수록 폴백 path 정정
2. **`measure_char_width_embedded` halfwidth_punct 분기에 narrow override** (line 997-1015) — DB fullwidth 잘못 기록 정정
   ```rust
   let is_narrow_unicode_punct = matches!(c, '\u{2018}' | '\u{2019}' | '\u{2027}');
   if is_narrow_unicode_punct && glyph_w >= mm.metric.em_size {
       (mm.metric.em_size as f64 * 0.3) as u16  // 0.5em → 0.3em
   } else if is_halfwidth_punct && glyph_w >= mm.metric.em_size {
       mm.metric.em_size / 2
   } else { glyph_w }
   ```

**1차 시각 검증 결과**:
- ✅ U+2018 정상화 — 휴먼명조 ‘ 의 advance 6px (0.3em) ✓
- ❌ U+2027 여전히 과대 — F-4 패턴 재현 가능성

**Fix C9 (WASM path 동기화 — F-4 학습 적용)**:
- 본질: `wasm_internals::measure_char_width_hwp` (line 604) 가 1차 내장 메트릭 → 2차 한글 → **3차 JS Canvas 폴백** 에서 `is_narrow_punctuation` 분기 누락. 휴먼명조 U+2027 (DB 글리프 미수록) 케이스가 JS 측정값 그대로 0.5em 폴백.
- Fix: 2차와 3차 사이에 narrow punctuation 분기 추가 ([text_measurement.rs:614-624](rhwp/src/renderer/layout/text_measurement.rs))
   ```rust
   // 2차: 한글 음절 → ...
   if super::is_narrow_punctuation(c) {
       return font_size * 0.3;
   }
   // 3차: JS 폴백
   ```

#### 검증 결과
- cargo test --lib: 1230/1230 (native, WASM 코드 제외) ✓
- WASM dev 빌드 성공 ✓
- 사용자 시각 검증:
  - ✅ 공직기강 page 11 가운뎃점 "선제적‧적극적", "갑질‧비위" — 한컴 정합 ✓
  - ✅ 여는 따옴표 (U+2018) "‘집행부 지휘부-도의회 의장단’" — 유지 ✓
  - ✅ 다른 punctuation (콤마, 마침표, 괄호) — 변경 없음 ✓
  - ✅ 다른 폰트 (맑은 고딕, 함초롬바탕) — 변경 없음 ✓

변경: `src/renderer/layout/text_measurement.rs`

## 5/18 학습 framework (3개 추가, 5/17 14개 + 5/18 3개 = 누적 17개)

### 15. 디폴트 pagination 엔진 = TypesetEngine (Paginator 아님)
`rendering.rs:1142-1170` 에서 `RHWP_USE_PAGINATOR=1` env 없으면 `TypesetEngine::typeset_section()` 호출. Paginator (engine.rs) 는 fallback 경로. **engine.rs 에만 fix 적용 시 디폴트 케이스 누락**. typeset.rs 의 동일 시멘틱 위치 동시 정합 필수. v_offset_px 차감 누락이 이 패턴.

### 16. F-4 패턴 일반화 — 모든 char/layout fix 는 native + WASM 두 path 동시 점검
F-4 (목차 페이지 번호) 에서 발견된 native (EmbeddedTextMeasurer) vs WASM (WasmTextMeasurer 경유 wasm_internals::measure_char_width_hwp) 분기 패턴이 C9 에서 재현. **native 만 fix 시 SVG 검증 OK / WASM Canvas NG**. 5/17 학습 1, 2 가 5/18 실제 재현으로 확정. 모든 char_width / position 영역 fix 의 영구 정책으로 격상.

### 17. inspect 도구로 메트릭 DB 잠재 본질 발견
가설 "휴먼명조 U+2018/2019 DB fullwidth 잘못 기록" 을 `examples/inspect_punct_widths.rs` (1회용 진단 도구) 로 정밀 측정 후 확정. 폰트 별 char 폭 직접 dump 패턴은 본가 메트릭 영역 작업 (F-6/F-8) 에도 재사용 가능. **가설 → 정밀 측정 → 확정 → fix → 검증** 절차 정착.

## 본가 PR 후보 갱신 (5/22 이후 도큐먼트화)

### ★★★★ 본가 가치 최상 (추가)
- **TypesetEngine + Paginator 시멘틱 동기화** (5/18 페이지 분할) — anchored 표 v_offset_px 차감이 typeset.rs 에 누락된 패턴. 양 엔진 동시 점검 필수 영역 도큐먼트화.
- **메트릭 DB U+2018/U+2019 fullwidth 정정 + native+WASM is_narrow_punctuation 동기화** (C8+C9) — 휴먼명조/HY 시리즈 4개 폰트 DB 자료 정정 + 폴백 path 일관성 정합. 본가 메트릭 정정 후보 (F-6/F-8 영역 인접).
- **paragraph_layout inline TAC `Control::Table` 분기 추가** (Y-1 후속) — Form/Shape/Picture/Equation 만 처리하던 inline TAC 분기에 Table 케이스 추가. 본가 paragraph_layout 영역.

### 본가 영역 보류 (변동 없음)
- estimate_text_width 5~15% 과대 추정 (X 진짜 본질)
- FieldMarkerType::Numbering variant 부재 (Y-2 inner mech, CRDT 보호)
- F-6/F-8 폰트 글리프 미세 차이

## 작업 환경
- 작업 브랜치: `local/capstone-fixes`
- 권한 정책: CAPSTONE-WORKFLOW.md 5/16 영구화 + 5/17 시각 검증 5단계 적용
- 검증 절차: cargo check → test → SVG → wasm-pack build --dev → **사용자 rhwp-studio 시각 검증** (5/17 학습 1, 2 정합)

## 5/22 마감 prep 영역

### 마일스톤 달성 (공직기강 hwp 시각 정합)
- A-4, F-1, F-4, F-3=Y-1, Y-1 후속, 페이지 분할, C8+C9 모두 완료
- 한컴 2024 와 시각 일치 (사용자 시각 검증 OK)
- 주무관님 5/18 보고 마일스톤 충족

### 보류 (캡스톤 마감 내 어려움)
- F-6 (◊ 다이아몬드 미세) — 폰트 메트릭 영역, 본가 협의
- F-8 (폰트 메트릭 미세) — 본가 협의
- 스마트행정팀 hwpx 잔여 issue — 다음 세션
